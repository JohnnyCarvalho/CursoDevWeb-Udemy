/**
 * Nesse exemplo vamos calcular a área de uma circunferência com o objeto Match
 */

const raio  = 5.1;
const area = Math.PI * Math.pow(raio, 2);

console.log(area.toFixed(2));
console.log(typeof Math);